# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME="powerlevel10k/powerlevel10k"

plugins=(
	asdf
	bundler
	common-aliases
	eza
	fzf
	rails
	git
	alias-tips
	zsh-autosuggestions
	zsh-syntax-highlighting
)

# Configure eza plugin
zstyle ':omz:plugins:eza' 'dirs-first' yes
zstyle ':omz:plugins:eza' 'git-status' yes
zstyle ':omz:plugins:eza' 'icons' yes

# Skip all aliases in lib files
zstyle ':omz:lib:*' aliases no

# Enable cattpuccin theme for zsh-syntax-highlighting
source $ZSH/custom/plugins/catppuccin-zsh-syntax-highlighting/themes/catppuccin_mocha-zsh-syntax-highlighting.zsh

source $ZSH/oh-my-zsh.sh

# catppuccin theme for eza
export LS_COLORS="$(vivid generate catppuccin-mocha)"

# catppuccin theme for fzf
export FZF_DEFAULT_OPTS=" \
--color=bg+:#313244,bg:#1e1e2e,spinner:#f5e0dc,hl:#f38ba8 \
--color=fg:#cdd6f4,header:#f38ba8,info:#cba6f7,pointer:#f5e0dc \
--color=marker:#b4befe,fg+:#cdd6f4,prompt:#cba6f7,hl+:#f38ba8 \
--color=selected-bg:#45475a \
--multi"

export LANG=en_US.UTF-8

{{ if .work }}
alias zshconfig="code ~/.zshrc"
alias szsh="source ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"
alias jc="cd ~/Code/jeancaisse"
alias gen="bin/rake autogenerated_files:generate"
alias genc="bin/rake autogenerated_files:clean && bin/rake autogenerated_files:generate"
alias bra="bin/rspec-auto"
alias braci="bin/rspec-auto --ci"
alias braciff="bin/rspec-auto --ci --fail-fast"
alias braff="bin/rspec-auto --fail-fast"
alias brcog="bin/rake code_owners:generate"
alias brof="bin/rspec --only-failures"
alias brnf="bin/rspec --next-failure "
alias br="bin/rspec"
alias brs="bin/rails s"
alias brc="bin/rails c"
alias tss="RAILS_ENV=test bin/rails db:sync_structure"

gqs() {
  git checkout $(git branch --sort=-committerdate --format="%(refname:short)" | fzf)
}


export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=yes
export PATH="$(brew --prefix)/opt/grep/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/findutils/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/gnu-sed/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/coreutils/libexec/gnubin:$PATH"
export NODE_OPTIONS=--max-old-space-size=8192
export PRY=1
export DD_TRACE_ENABLED=false
export PATH="$(brew --prefix)/opt/grep/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/findutils/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/gnu-sed/libexec/gnubin:$PATH"
export PATH="$(brew --prefix)/opt/coreutils/libexec/gnubin:$PATH"
# export HOMEBREW_NO_AUTO_UPDATE=1

source /Users/f.plante/Code/jeancaisse/docker.env
#ssh-add --apple-load-keychain &>/dev/null
{{ end }}




# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
